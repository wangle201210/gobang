services:
  gobang:
    image: iwangle/gobang:latest
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - gobang-net

networks:
  gobang-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16
